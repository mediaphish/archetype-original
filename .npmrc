# Optimize npm install performance
prefer-offline=true
progress=false
audit=false
fund=false

# Skip optional dependencies (like Puppeteer) during install
# Puppeteer is only needed for local pre-rendering, not Vercel builds
optional=false

