/**
 * Placeholder for future per-channel analytics (API ingest → join ao_scheduled_posts.external_id).
 * Call sites can import attachScheduledPostMetricsStub when metrics sync exists.
 */

/** @param {{ id?: string, platform?: string, external_id?: string }} _row */
export function attachScheduledPostMetricsStub(_row) {
  return null;
}
